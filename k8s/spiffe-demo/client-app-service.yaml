apiVersion: v1
kind: Service
metadata:
  name: spiffe-client-app
  namespace: spiffe-demo
  labels:
    app: spiffe-client-app
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: spiffe-client-app
  type: ClusterIP
