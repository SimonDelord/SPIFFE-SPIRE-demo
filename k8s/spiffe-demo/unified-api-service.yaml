apiVersion: v1
kind: Service
metadata:
  name: unified-api
  namespace: spiffe-demo
  labels:
    app: unified-api
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: unified-api
  type: ClusterIP
